{% extends 'page.njk' %}
{% from 'hero.njk' import hero  %}

{% block body %}

  {{ hero(
    eyebrow = date | date("MMMM d, yyyy"),
    heading = title,
    paragraph = description
  ) }}

  {# Content #}
  <section class="container-narrow prose prose-indigo lg:prose-lg section-spacing">
    {{ content | safe }}
  </section>

  {% include 'contact-cta.njk' %}

{% endblock body %}
